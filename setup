git config --global user.email yassinerahhaoui12@gmail.com
git config --global user.name yassinerahhaui

mkdir ~/Workspace

curl -fsSL https://get.docker.com/rootless | sh
export

echo "export PATH=$HOME/bin:$HOME/.local/bin:/usr/local/bin:$PATH\n$(cat ~/.zshrc)" > ~/.zshrc
echo "exec zsh\n$(cat ~/.bashrc)" > ~/.bashrc

